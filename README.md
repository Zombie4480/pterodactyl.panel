# Pterodactyl Minecraft Hosting Platform

Полнофункциональная платформа для хостинга Minecraft серверов на панели Pterodactyl.

## 📋 Содержание

1. [Введение](#введение)
2. [Требования](#требования)
3. [Установка](#установка)
4. [Использование](#использование)
5. [Конфигурация](#конфигурация)
6. [Скрипты управления](#скрипты-управления)
7. [Резервное копирование](#резервное-копирование)

---

## Введение

Это полная реализация хостинга Minecraft на панели Pterodactyl, включающая:

- ✅ Pterodactyl Panel (главная панель управления)
- ✅ Pterodactyl Wings (демон управления серверами)
- ✅ MySQL база данных
- ✅ Redis кеш
- ✅ Docker образ для Minecraft серверов
- ✅ Egg конфигурация для Minecraft Java Edition
- ✅ Скрипты управления серверами
- ✅ Систему резервного копирования

---

## Требования

### Минимальные требования:

- **ОС**: Linux (Ubuntu 20.04+, CentOS 7+, Debian 10+)
- **CPU**: 2 ядра
- **RAM**: 4 GB минимум (рекомендуется 8+ GB)
- **Диск**: 20+ GB свободного места
- **Docker**: 20.10+
- **Docker Compose**: 1.29+

### Рекомендуемые требования:

- **CPU**: 4+ ядра
- **RAM**: 16+ GB
- **Диск**: SSD с 100+ GB свободного места
- **Интернет**: Стабильное соединение (1 Mbps+)

---

## Установка

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Zombie4480/pterodactyl.panel.git
cd pterodactyl.panel
```

### Шаг 2: Установка зависимостей

```bash
chmod +x scripts/*.sh
./scripts/init_pterodactyl.sh
```

Скрипт автоматически установит Docker и Docker Compose, если они не установлены.

### Шаг 3: Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up`.

---

## Использование

### Первый запуск Pterodactyl Panel

1. Откройте браузер и перейдите на `http://localhost`
2. Создайте аккаунт администратора
3. Перейдите в Admin Panel
4. Создайте новое Гнездо (Nest) для Minecraft

### Импорт Minecraft Egg

1. В Админ-панели перейдите на **Nests** → **Import Egg**
2. Загрузите файл `pterodactyl/minecraft_egg.json`
3. Выберите гнездо и нажмите Import

### Создание сервера Minecraft

1. В Админ-панели создайте новый **Node** (если его нет)
   - Name: `Local Node`
   - FQDN: `localhost`
   - Scheme: `http`
   - Port: `8080`

2. Создайте новый **Server**:
   - Выберите Nest: `Minecraft Java Edition`
   - Выберите Egg: `Minecraft`
   - Выберите память и диск
   - Нажмите Create Server

3. Включите режим RCON для удаленного управления

---

## Конфигурация

### Pterodactyl Panel

Конфигурационный файл: `docker-compose.yml`

Основные переменные окружения:

```yaml
APP_URL=http://localhost           # URL панели
APP_ENV=production                 # Окружение
DB_HOST=mysql                      # Хост БД
DB_USERNAME=pterodactyl            # Пользователь БД
DB_PASSWORD=pterodactyl_password   # Пароль БД
REDIS_HOST=redis                   # Redis хост
```

### Minecraft Server

Конфигурация сервера: `minecraft/server.properties`

Основные параметры:

```properties
max-players=20              # Максимум игроков
difficulty=normal           # Сложность
gamemode=survival          # Режим игры
pvp=true                   # PvP
enable-rcon=true           # Включить RCON
```

### Docker

Параметры для контейнеров настраиваются в `docker-compose.yml`:

- **Память Minecraft**: `-Xmx1024M -Xms512M` (1024M = 1 GB)
- **Версия Java**: `openjdk:17-jdk-slim`

---

## Скрипты управления

### Запуск сервера

```bash
./scripts/start_minecraft_server.sh <server_id> <memory> <max_players>

# Пример:
./scripts/start_minecraft_server.sh server1 2048 50
```

### Управление сервером

```bash
./scripts/manage_server.sh <action> <server_id>

# Доступные действия:
./scripts/manage_server.sh start server1       # Запуск
./scripts/manage_server.sh stop server1        # Остановка
./scripts/manage_server.sh restart server1     # Перезагрузка
./scripts/manage_server.sh status server1      # Статус
./scripts/manage_server.sh logs server1        # Логи
./scripts/manage_server.sh backup server1      # Резервная копия
```

---

## Резервное копирование

### Автоматическое резервное копирование

Мир сохраняется автоматически в `servers/<server_id>/world/`

### Ручное создание резервной копии

```bash
./scripts/manage_server.sh backup server1
```

Копия сохранится в `backups/server1-<timestamp>/`

### Восстановление из резервной копии

```bash
# Остановить сервер
./scripts/manage_server.sh stop server1

# Восстановить мир
cp -r backups/server1-<timestamp>/world servers/server1/

# Запустить сервер
./scripts/manage_server.sh start server1
```

---

## Управление Docker контейнерами

### Просмотр контейнеров

```bash
docker-compose ps
```

### Просмотр логов

```bash
# Все контейнеры
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f pterodactyl-panel
docker-compose logs -f pterodactyl-wings
docker-compose logs -f mysql
docker-compose logs -f redis
```

### Остановка контейнеров

```bash
docker-compose down
```

### Перезагрузка контейнеров

```bash
docker-compose restart
```

---

## Структура проекта

```
pterodactyl.panel/
├── docker/                          # Docker конфигурации
│   └── Dockerfile.minecraft        # Dockerfile для Minecraft
├── pterodactyl/                    # Конфигурации Pterodactyl
│   ├── minecraft_egg.json         # Egg конфигурация
│   ├── var/                       # Данные приложения
│   ├── storage/                   # Хранилище
│   └── public/                    # Публичные файлы
├── minecraft/                      # Конфигурация Minecraft
│   └── server.properties          # Свойства сервера
├── scripts/                        # Скрипты управления
│   ├── init_pterodactyl.sh       # Инициализация
│   ├── start_minecraft_server.sh  # Запуск сервера
│   └── manage_server.sh           # Управление сервером
├── config/                         # Конфигурационные файлы
├── servers/                        # Директория серверов
├── backups/                        # Резервные копии
├── docker-compose.yml             # Docker Compose конфигурация
└── README.md                      # Документация
```

---

## Порты

| Сервис | Порт | Описание |
|--------|------|---------|
| Pterodactyl Panel | 80/443 | HTTP/HTTPS |
| Wings Daemon | 8080/8443 | HTTP/HTTPS |
| MySQL | 3306 | База данных |
| Redis | 6379 | Кеш |
| Minecraft | 25565 | Игровой сервер |
| RCON | 25575 | Удаленное управление |

---

## Устранение неполадок

### Контейнеры не запускаются

```bash
# Проверить логи
docker-compose logs

# Перестроить контейнеры
docker-compose down
docker-compose up -d --build
```

### Панель не доступна

```bash
# Проверить статус контейнера
docker-compose ps pterodactyl-panel

# Проверить логи
docker-compose logs pterodactyl-panel
```

### Minecraft сервер не подключается

1. Проверить, что сервер запущен: `./scripts/manage_server.sh status server1`
2. Проверить логи: `./scripts/manage_server.sh logs server1`
3. Проверить firewall: `sudo ufw allow 25565/tcp`
4. Проверить port forwarding в маршрутизаторе

---

## Безопасность

### Рекомендации:

1. **Измените пароли по умолчанию** в `docker-compose.yml`:
   - DB_PASSWORD
   - RCON_PASSWORD
   - REDIS_PASSWORD

2. **Используйте HTTPS** в production:
   - Получите SSL сертификат (Let's Encrypt)
   - Обновите APP_URL в docker-compose.yml

3. **Настройте Firewall**:
   ```bash
   sudo ufw allow 22/tcp
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw allow 8080/tcp
   sudo ufw allow 8443/tcp
   sudo ufw enable
   ```

4. **Регулярные резервные копии**:
   ```bash
   # Ежедневное резервное копирование
   0 2 * * * cd /path/to/pterodactyl.panel && ./scripts/manage_server.sh backup server1
   ```

---

## Поддержка

### Полезные ссылки:

- [Pterodactyl Документация](https://pterodactyl.io/project/introduction.html)
- [Minecraft Документация](https://minecraft.wiki/)
- [Docker Документация](https://docs.docker.com/)
- [Paper MC](https://papermc.io/) - Оптимизированный сервер Minecraft

---

## Лицензия

MIT License - см. LICENSE файл

---

## Автор

Создано для хостинга Minecraft серверов на Pterodactyl Panel

**Контакты**: admin@pterodactyl.local

---

**Последнее обновление**: 09 декабря 2025
