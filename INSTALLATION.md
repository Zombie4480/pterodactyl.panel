# Pterodactyl Minecraft Hosting - Установка и Начало

## 🎯 Что это?

Полнофункциональная платформа для управления и хостинга Minecraft серверов на Pterodactyl Panel. Включает всё необходимое для запуска профессионального игрового хостинга.

## ⚡ Быстрый старт (5 минут)

### Шаг 1: Клонирование проекта
```bash
git clone https://github.com/Zombie4480/pterodactyl.panel.git
cd pterodactyl.panel
```

### Шаг 2: Запуск инициализации
```bash
chmod +x scripts/*.sh
./scripts/init_pterodactyl.sh
```

### Шаг 3: Проверка статуса
```bash
docker-compose ps
```

Все контейнеры должны быть в статусе `Up`.

### Шаг 4: Открыть панель
Откройте в браузере: **http://localhost**

---

## 📚 Основные компоненты

| Компонент | Адрес | Пользователь | Пароль |
|-----------|-------|-------------|--------|
| Pterodactyl Panel | http://localhost | admin | `создайте` |
| Wings Daemon | http://localhost:8080 | - | - |
| MySQL | localhost:3306 | pterodactyl | pterodactyl_password |
| Redis | localhost:6379 | - | - |
| Minecraft | localhost:25565 | - | - |

---

## 🎮 Создание первого Minecraft сервера

### Этап 1: Импорт Minecraft Egg

1. Откройте Pterodactyl Panel (http://localhost)
2. Перейдите в **Admin Panel** → **Nests**
3. Нажмите **Import Egg**
4. Загрузите файл: `pterodactyl/minecraft_egg.json`
5. Выберите гнездо и нажмите **Import**

### Этап 2: Создание Node

1. В Admin Panel → **Nodes**
2. Нажмите **New Node**
3. Заполните параметры:
   - **Name**: `Local Node`
   - **FQDN**: `localhost`
   - **Scheme**: `http`
   - **Daemon Port**: `8080`
   - **Memory**: `8192`
   - **Disk**: `50000`
4. Сохраните

### Этап 3: Создание сервера

1. В Admin Panel → **Servers**
2. Нажмите **New Server**
3. Выберите:
   - **Owner**: создайте пользователя или выберите существующего
   - **Nest**: `Minecraft Java Edition`
   - **Egg**: `Minecraft`
   - **Node**: `Local Node`
4. Настройте параметры:
   - **Memory**: `2048 MB` (для теста)
   - **Disk**: `5000 MB`
   - **CPU**: `100%`
5. Создайте сервер

### Этап 4: Запуск сервера

1. Перейдите на страницу сервера
2. Нажмите **Start**
3. Дождитесь загрузки (1-2 минуты)
4. Подключитесь в Minecraft: `localhost:25565`

---

## 🛠️ Управление серверами через CLI

### Проверка статуса всех контейнеров
```bash
docker-compose ps
```

### Просмотр логов
```bash
# Все логи
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f pterodactyl-panel
docker-compose logs -f mysql
```

### Остановка всех сервисов
```bash
docker-compose down
```

### Перезагрузка
```bash
docker-compose restart
```

### Входящий в MySQL контейнер
```bash
docker-compose exec mysql mysql -u pterodactyl -pterodactyl_password pterodactyl
```

---

## 📁 Структура проекта

```
pterodactyl.panel/
├── docker-compose.yml              # Конфигурация Docker Compose
├── docker/                         # Docker Dockerfile's
│   ├── Dockerfile.minecraft       # Базовый Minecraft образ
│   └── Dockerfile.minecraft-optimized  # Оптимизированный образ
├── pterodactyl/                   # Конфиги Pterodactyl
│   ├── minecraft_egg.json        # Конфиг Minecraft Egg
│   ├── var/                      # Данные приложения
│   ├── storage/                  # Хранилище
│   └── public/                   # Публичные файлы
├── minecraft/                     # Конфиги Minecraft
│   ├── server.properties         # Свойства сервера
│   └── paper-global.yml          # Конфиг Paper Server
├── scripts/                       # Управляющие скрипты
│   ├── init_pterodactyl.sh      # Инициализация
│   ├── start_minecraft_server.sh # Запуск сервера
│   ├── manage_server.sh         # Управление сервером
│   └── update_and_migrate.sh    # Обновление и миграция
├── config/                        # Конфигурационные файлы
│   └── .env                      # Переменные окружения
├── servers/                       # Директория для серверов
├── backups/                       # Резервные копии
├── README.md                      # Полная документация
└── package.json                  # Метаданные проекта
```

---

## 🔐 Безопасность

### ⚠️ Важно перед продакшеном:

1. **Измените пароли по умолчанию**:
   ```bash
   nano docker-compose.yml
   # Измените:
   # - DB_PASSWORD
   # - RCON_PASSWORD
   # - REDIS_PASSWORD
   ```

2. **Включите HTTPS**:
   - Получите SSL сертификат (Let's Encrypt)
   - Обновите `APP_URL` в `docker-compose.yml`

3. **Настройте Firewall**:
   ```bash
   sudo ufw allow 22/tcp
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw allow 8080/tcp
   sudo ufw allow 8443/tcp
   sudo ufw allow 25565/tcp
   sudo ufw enable
   ```

4. **Создавайте резервные копии**:
   ```bash
   ./scripts/update_and_migrate.sh backup
   ```

---

## 📊 Мониторинг

### Проверка использования ресурсов
```bash
docker stats
```

### Проверка дискового пространства
```bash
df -h
```

### Просмотр процессов
```bash
docker-compose top
```

---

## 🔧 Устранение неполадок

### Проблема: Контейнеры не запускаются

**Решение**:
```bash
# Проверить ошибки
docker-compose logs

# Пересоздать контейнеры
docker-compose down
docker-compose up -d --build
```

### Проблема: Панель недоступна (http://localhost)

**Решение**:
```bash
# Проверить статус контейнера
docker-compose ps pterodactyl-panel

# Проверить логи
docker-compose logs pterodactyl-panel

# Перезагрузить
docker-compose restart pterodactyl-panel
```

### Проблема: Minecraft сервер не запускается

**Решение**:
```bash
# Проверить логи Wings
docker-compose logs pterodactyl-wings

# Проверить диск
df -h

# Проверить память
docker stats pterodactyl-wings
```

### Проблема: Не удалось подключиться к серверу

**Решение**:
1. Проверьте, что сервер запущен в админ-панели
2. Убедитесь, что порт 25565 открыт: `telnet localhost 25565`
3. Проверьте firewall: `sudo ufw status`
4. Проверьте логи сервера в админ-панели

---

## 📖 Подробная документация

Для полной документации смотрите **README.md**

Основные разделы:
- Установка и требования
- Конфигурация компонентов
- Управление серверами
- Резервное копирование
- Безопасность и оптимизация
- Устранение неполадок

---

## 🚀 Дополнительные команды

### Просмотр всех скриптов
```bash
ls -la scripts/
```

### Запуск Minecraft сервера напрямую
```bash
./scripts/start_minecraft_server.sh server1 2048 50
```

### Управление сервером
```bash
# Запуск
./scripts/manage_server.sh start server1

# Остановка
./scripts/manage_server.sh stop server1

# Перезагрузка
./scripts/manage_server.sh restart server1

# Статус
./scripts/manage_server.sh status server1

# Логи
./scripts/manage_server.sh logs server1

# Резервная копия
./scripts/manage_server.sh backup server1
```

### Обновление и миграция
```bash
# Полная резервная копия
./scripts/update_and_migrate.sh backup

# Обновить Panel
./scripts/update_and_migrate.sh update-panel

# Обновить Wings
./scripts/update_and_migrate.sh update-wings

# Показать статус
./scripts/update_and_migrate.sh status

# Помощь
./scripts/update_and_migrate.sh help
```

---

## 📞 Поддержка

### Полезные ссылки:

- 📖 [Pterodactyl Docs](https://pterodactyl.io)
- 🎮 [Minecraft Wiki](https://minecraft.wiki)
- 📦 [Docker Docs](https://docs.docker.com)
- 📄 [Paper MC](https://papermc.io)

---

## 📝 Часто задаваемые вопросы

**В: Можно ли запустить несколько Minecraft серверов?**
Да! Pterodactyl позволяет запускать неограниченное количество серверов через админ-панель.

**В: Как изменить память сервера?**
В админ-панели → Сервер → Settings → Resource Management

**В: Как установить плагины?**
1. Подключитесь к серверу через SFTP (учетные данные в админ-панели)
2. Загрузите плагины в папку `plugins/`
3. Перезагрузите сервер

**В: Как создать резервную копию?**
```bash
./scripts/manage_server.sh backup server1
```

**В: Как восстановить из резервной копии?**
```bash
./scripts/update_and_migrate.sh restore backups/full-20231209-120000
```

---

## 🎉 Готово!

Поздравляем! Ваша платформа Pterodactyl Minecraft хостинга полностью готова к использованию.

**Начните с**:
1. 📖 Прочитайте README.md для подробной документации
2. 🎮 Создайте первый Minecraft сервер через админ-панель
3. 🔐 Измените пароли по умолчанию
4. 💾 Создайте резервную копию
5. 🚀 Пригласите игроков!

---

**Версия**: 1.0.0  
**Дата**: 09 декабря 2025  
**Автор**: Pterodactyl Admin
